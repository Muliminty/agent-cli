# Agent CLI 实战教程

> 长效运行智能体CLI工具 - 详细实战指南

## 📋 教程概述

本教程将带你从零开始，完整地使用 Agent CLI 开发一个实际项目。通过本教程，你将掌握：

1. **环境搭建**：安装、配置和构建 Agent CLI
2. **项目初始化**：创建不同类型项目的方法和技巧
3. **AI集成**：配置和使用多个AI提供商
4. **开发工作流**：增量开发、测试和进度跟踪
5. **Web界面**：使用可视化工具管理项目
6. **故障排除**：常见问题解决方法

## 🛠️ 环境准备

### 1. 克隆项目

```bash
# 克隆项目到本地
git clone https://github.com/your-username/agent-cli.git
cd agent-cli
```

### 2. 安装依赖

```bash
# 安装项目依赖
npm install

# 可选：检查依赖版本
npm list --depth=0
```

### 3. 构建项目

```bash
# 开发构建（监视模式）
npm run dev

# 生产构建
npm run build

# 全局安装CLI工具
npm link
```

### 4. 验证安装

```bash
# 检查版本
agent-cli --version

# 查看所有命令
agent-cli --help

# 测试基本功能
agent-cli init --help
```

## 🔧 AI提供商配置

### 1. 获取API密钥

需要准备的API密钥：
- **Anthropic Claude**: [平台地址](https://console.anthropic.com)
- **OpenAI GPT**: [平台地址](https://platform.openai.com)
- **DeepSeek**: [平台地址](https://platform.deepseek.com)
- **智谱AI**: [平台地址](https://open.bigmodel.cn)

### 2. 设置环境变量

```bash
# macOS/Linux - 添加到 ~/.bashrc 或 ~/.zshrc
export ANTHROPIC_API_KEY="sk-ant-api..."
export OPENAI_API_KEY="sk-..."
export DEEPSEEK_API_KEY="sk-..."
export ZHIPU_API_KEY="your-zhipu-api-key"

# Windows - 系统环境变量或PowerShell
$env:ANTHROPIC_API_KEY="sk-ant-api..."
```

### 3. 配置文件示例

创建 `agent.config.json`：

```json
{
  "project": {
    "name": "my-web-app",
    "description": "现代化的React应用",
    "type": "web-app",
    "techStack": ["react", "typescript", "tailwind"],
    "version": "1.0.0"
  },
  "agent": {
    "model": "claude-3-5-sonnet",
    "ai": {
      "defaultProvider": "anthropic",
      "providers": {
        "anthropic": {
          "provider": "anthropic",
          "enabled": true,
          "apiKey": "${ANTHROPIC_API_KEY}"
        },
        "openai": {
          "provider": "openai",
          "enabled": true,
          "apiKey": "${OPENAI_API_KEY}"
        },
        "deepseek": {
          "provider": "deepseek",
          "enabled": true,
          "apiKey": "${DEEPSEEK_API_KEY}"
        },
        "zhipu": {
          "provider": "zhipu",
          "enabled": true,
          "apiKey": "${ZHIPU_API_KEY}"
        }
      }
    },
    "contextMonitoring": {
      "enabled": true,
      "warningThreshold": 0.8,
      "autoSummarize": true
    }
  },
  "testing": {
    "framework": "puppeteer",
    "headless": true,
    "timeout": 30000
  }
}
```

## 🚀 项目初始化实战

### 1. 交互式创建项目

```bash
# 启动交互式创建向导
agent-cli init my-todo-app

# 按照提示依次输入：
# - 项目名称: my-todo-app
# - 项目路径: ./my-todo-app (默认)
# - 项目描述: "一个现代化的Todo应用"
# - 项目模板: web-app
# - 初始化Git仓库: 是
# - 创建初始功能列表: 是
# - Git用户名: 你的名字
# - Git用户邮箱: 你的邮箱
```

### 2. 非交互式创建

```bash
# 快速创建API服务项目
agent-cli init user-management-api \
  --template api-service \
  --description "用户管理REST API" \
  --no-git \
  --yes

# 创建库项目
agent-cli init utils-library \
  --template library \
  --description "工具函数库" \
  --skip-features \
  --yes
```

### 3. 查看初始化结果

```bash
# 进入项目目录
cd my-todo-app

# 查看生成的文件结构
ls -la

# 查看项目状态
agent-cli status

# 查看进度文件
cat claude-progress.txt
cat feature-list.json
```

## 📊 增量开发工作流

### 1. 理解功能列表

```bash
# 查看所有功能
agent-cli status --detailed

# 查看功能详情（JSON格式）
agent-cli status --format json | jq '.features[] | {id, name, status, priority}'
```

### 2. 获取下一个功能

```bash
# 获取推荐的下一个功能
agent-cli next

# 输出示例：
# 🎯 推荐功能: 项目基础结构搭建 (feature-001)
# 📝 描述: 创建项目目录和配置文件
# 🔗 依赖: 无
# 📋 步骤:
#   1. 创建目录结构
#   2. 配置TypeScript编译
#   3. 设置构建工具配置
#   4. 配置开发服务器
#   5. 添加基础依赖

# 显示功能依赖关系
agent-cli next --show-dependencies
```

### 3. 实现功能

#### 方法A：手动实现
```bash
# 1. 查看功能详情
agent-cli status --feature-id "feature-001"

# 2. 按照步骤实现代码
# 3. 运行测试验证
agent-cli test

# 4. 标记功能完成
# （系统自动标记，或通过进度跟踪器更新）
```

#### 方法B：使用AI助手实现
```bash
# 1. 准备功能描述
FEATURE_DESC=$(agent-cli status --format json | jq -r '.features[0].description')

# 2. 使用AI生成代码（示例）
# 通过AI API或CLI工具生成实现代码
# 3. 验证生成的代码
agent-cli test
```

### 4. 测试和验证

```bash
# 运行所有测试
agent-cli test

# 运行特定测试套件
agent-cli test --suites "tests/todo-basic.json"

# 生成测试报告
agent-cli test --report --output ./test-report.html

# 查看测试覆盖率
# （需要配置测试覆盖率工具）
```

### 5. 提交和进度更新

```bash
# Git提交（如果启用autoCommit）
git status

# 手动提交
git add .
git commit -m "feat: 实现项目基础结构搭建"

# 查看更新后的进度
agent-cli status
```

## 🌐 Web界面使用

### 1. 启动Web服务器

```bash
# 启动默认端口(3000)
agent-cli serve

# 指定端口启动
agent-cli serve --port 8080

# 后台运行
agent-cli serve --port 3000 --daemon

# 查看服务器日志
tail -f logs/server.log
```

### 2. 访问Web界面

```
浏览器访问: http://localhost:3000
```

### 3. 主要功能模块

**仪表板**：
- 项目概览和进度统计
- 功能状态可视化
- 实时活动日志

**项目管理**：
- 功能列表查看和筛选
- 状态更新和标记
- 依赖关系可视化

**AI会话管理**：
- 上下文监控和预警
- Token使用统计
- 会话历史记录

**测试结果**：
- 测试运行状态
- 失败测试详情
- 测试报告生成

### 4. API接口使用

```bash
# 查看Web API文档
open http://localhost:3000/api-docs

# 使用curl测试API
curl http://localhost:3000/api/project/status
curl http://localhost:3000/api/features
curl http://localhost:3000/api/test/results
```

## 🔧 高级功能实战

### 1. 配置管理

```bash
# 查看当前配置
agent-cli config --list

# 设置配置值
agent-cli config --set "agent.model=claude-3-5-sonnet"
agent-cli config --set "agent.temperature=0.7"
agent-cli config --set "testing.timeout=60000"

# 导出配置
agent-cli config --export ./backup-config.json

# 导入配置
agent-cli config --import ./backup-config.json
```

### 2. 报告生成

```bash
# 生成进度报告
agent-cli report --type progress \
  --format html \
  --output ./reports/progress-$(date +%Y%m%d).html

# 生成测试报告
agent-cli report --type test \
  --format json \
  --output ./reports/test-results.json

# 生成综合报告
agent-cli report --type comprehensive \
  --format markdown \
  --output ./reports/full-report.md
```

### 3. 上下文监控

```bash
# 检查消息token使用
agent-cli context --input ./conversation.json --threshold 0.75

# 监控实时会话
agent-cli context --monitor \
  --output ./context-logs/session-$(date +%H%M%S).log

# 生成会话总结
agent-cli context --summarize \
  --input ./long-conversation.json \
  --output ./summaries/summary.md
```

### 4. 模板系统

```bash
# 查看可用模板
agent-cli template list

# 查看模板详情
agent-cli template info init-prompt

# 渲染模板
agent-cli template render init-prompt \
  --output ./custom-prompt.md \
  --data '{
    "projectName": "电商平台",
    "projectDescription": "B2C电商网站",
    "techStack": ["react", "typescript", "nodejs"]
  }'

# 添加自定义模板
agent-cli template add ./my-custom-template.md
```

## 🚨 故障排除指南

### 1. 安装问题

**问题**: `npm install` 失败
```
解决方案：
1. 清理npm缓存：npm cache clean --force
2. 删除node_modules：rm -rf node_modules
3. 重新安装：npm install
4. 检查Node版本：node --version (需要>=18.0.0)
```

**问题**: `agent-cli` 命令未找到
```
解决方案：
1. 检查全局安装：which agent-cli
2. 重新链接：npm link
3. 使用本地路径：./bin/agent-cli
4. 添加路径到PATH
```

### 2. AI服务问题

**问题**: API调用失败
```
检查步骤：
1. 验证API密钥：echo $ANTHROPIC_API_KEY
2. 检查网络连接：curl -I https://api.anthropic.com
3. 查看错误日志：tail -f logs/agent-cli.log
4. 切换提供商：agent-cli config --set "agent.ai.defaultProvider=openai"
```

**问题**: Token超出限制
```
解决方案：
1. 启用上下文监控：agent-cli config --set "agent.contextMonitoring.enabled=true"
2. 降低阈值：agent-cli config --set "agent.contextMonitoring.warningThreshold=0.7"
3. 启用自动总结：agent-cli config --set "agent.contextMonitoring.autoSummarize=true"
```

### 3. 测试问题

**问题**: Puppeteer浏览器无法启动
```
解决方案：
1. 安装Chrome：brew install google-chrome
2. 指定浏览器路径：agent-cli config --set "testing.browserPath=/usr/bin/google-chrome"
3. 使用无头模式：agent-cli config --set "testing.headless=true"
4. 增加超时时间：agent-cli config --set "testing.timeout=60000"
```

**问题**: 测试超时
```
解决方案：
1. 增加超时：agent-cli test --timeout 60000
2. 简化测试：检查测试文件复杂度
3. 分步运行：agent-cli test --suites "tests/basic.json"
```

### 4. Git集成问题

**问题**: Git操作失败
```
检查步骤：
1. 验证Git配置：git config --list
2. 检查仓库权限
3. 查看错误详情：agent-cli --verbose status
4. 禁用Git集成：agent-cli config --set "git.autoCommit=false"
```

## 📈 性能优化

### 1. 构建优化

```bash
# 清理构建缓存
rm -rf dist/*

# 重新构建
npm run build

# 生产环境优化构建
NODE_ENV=production npm run build
```

### 2. 测试优化

```json
{
  "testing": {
    "headless": true,
    "timeout": 30000,
    "parallel": true,
    "maxWorkers": 4,
    "cache": true
  }
}
```

### 3. 缓存配置

```bash
# 启用文件缓存
agent-cli config --set "features.enableCaching=true"

# 设置缓存目录
agent-cli config --set "paths.cacheDir=.agent-cache"

# 清理缓存
rm -rf .agent-cache
```

## 🎯 最佳实践总结

### 1. 项目管理
- 每个功能保持原子性，独立可测试
- 定期运行测试，确保代码质量
- 使用Git提交模板，保持提交信息一致性
- 定期生成项目报告，跟踪进度

### 2. AI使用
- 监控上下文使用，避免超出限制
- 使用多个AI提供商，提高可用性
- 配置合理的温度和token限制
- 保存重要会话记录

### 3. 团队协作
- 统一团队配置和模板
- 建立代码审查流程
- 集成到CI/CD流程
- 定期分享使用经验

### 4. 维护建议
- 定期更新依赖版本
- 备份重要配置文件
- 监控系统资源使用
- 收集用户反馈改进

## 🔗 下一步学习

### 深入学习
- 阅读完整用户指南：`!doc/USER_GUIDE.md`
- 查看项目架构：`!doc/避免上下文爆炸的开发工作流方案.md`
- 研究示例项目：`examples/web-app/`

### 进阶主题
1. **自定义模板开发**
2. **插件系统扩展**
3. **CI/CD集成**
4. **监控和告警系统**

### 社区资源
- GitHub仓库：https://github.com/your-username/agent-cli
- 问题追踪：https://github.com/your-username/agent-cli/issues
- 讨论区：https://github.com/your-username/agent-cli/discussions

---

## 📝 实战练习

### 练习1：创建个人博客系统
```bash
# 目标：使用Agent CLI开发一个完整的博客系统
# 步骤：
# 1. 初始化项目：agent-cli init my-blog --template web-app
# 2. 配置AI提供商和测试
# 3. 实现核心功能：文章管理、用户认证、评论系统
# 4. 部署到生产环境
```

### 练习2：API网关开发
```bash
# 目标：开发一个微服务API网关
# 步骤：
# 1. 初始化项目：agent-cli init api-gateway --template api-service
# 2. 配置路由、认证、限流功能
# 3. 实现服务发现和负载均衡
# 4. 性能测试和优化
```

### 练习3：CLI工具开发
```bash
# 目标：开发一个自定义CLI工具
# 步骤：
# 1. 初始化项目：agent-cli init my-cli-tool --template library
# 2. 设计命令结构和参数解析
# 3. 实现核心业务逻辑
# 4. 打包和发布到npm
```

---

*教程版本: 1.0.0*
*最后更新: 2026-02-18*
*建议配合"快速开始.md"和"USER_GUIDE.md"一起使用*