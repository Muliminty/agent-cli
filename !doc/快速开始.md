# Agent CLI 快速开始指南

> 长效运行智能体CLI工具 - 快速上手教程

## 📋 概述

Agent CLI 是一个AI增强的脚手架和项目管理CLI工具，支持：
- 🚀 **项目初始化**：基于模板快速创建项目结构
- 📊 **进度跟踪**：双轨方案实现，结构化进度管理
- 🔄 **增量开发**：每次只实现一个功能，保持原子性提交
- 🤖 **多AI提供商**：Claude、GPT、DeepSeek、智谱AI
- 🌐 **Web可视化界面**：实时进度监控和项目管理
- ✅ **自动化测试**：端到端验证和报告生成

## 🚀 5分钟快速上手

### 步骤1：安装与构建

```bash
# 1. 进入项目目录
cd /Users/muliminty/project/agent-cli

# 2. 安装依赖
npm install

# 3. 构建项目
npm run build

# 4. 全局安装（可选，便于在任何位置使用）
npm link

# 5. 验证安装
agent-cli --version
agent-cli --help
```

### 步骤2：配置AI提供商

在项目目录创建或修改 `agent.config.json`，设置API密钥：

```json
{
  "agent": {
    "ai": {
      "defaultProvider": "anthropic",
      "providers": {
        "anthropic": {
          "provider": "anthropic",
          "enabled": true,
          "apiKey": "${ANTHROPIC_API_KEY}"
        },
        "openai": {
          "provider": "openai",
          "enabled": true,
          "apiKey": "${OPENAI_API_KEY}"
        },
        "deepseek": {
          "provider": "deepseek",
          "enabled": true,
          "apiKey": "${DEEPSEEK_API_KEY}"
        }
      }
    }
  }
}
```

设置环境变量：
```bash
# macOS/Linux
export ANTHROPIC_API_KEY="your-anthropic-api-key"
export OPENAI_API_KEY="your-openai-api-key"
export DEEPSEEK_API_KEY="your-deepseek-api-key"

# Windows (PowerShell)
$env:ANTHROPIC_API_KEY="your-anthropic-api-key"
```

### 步骤3：创建第一个项目

```bash
# 交互式创建（推荐）
agent-cli init my-first-project

# 非交互式快速创建
agent-cli init todo-app \
  --template web-app \
  --description "Todo应用项目" \
  --yes
```

**可用模板**：
- `web-app` - React + TypeScript + Vite Web应用
- `api-service` - Node.js + Express + TypeScript API服务
- `library` - TypeScript库项目

### 步骤4：查看项目状态

```bash
# 进入项目目录
cd my-first-project

# 查看总体状态
agent-cli status

# 详细状态（JSON格式）
agent-cli status --detailed --format json
```

### 步骤5：开始增量开发

```bash
# 获取下一个推荐功能
agent-cli next

# 查看功能详情和依赖
agent-cli next --show-dependencies
```

**开发工作流**：
1. `agent-cli next` 获取功能描述
2. 实现功能（手动或使用AI助手）
3. `agent-cli test` 验证功能
4. 重复步骤1-3

## 📁 项目结构说明

初始化后的项目结构：
```
my-first-project/
├── src/                    # 源代码
│   ├── components/        # React组件（web-app模板）
│   ├── pages/            # 页面组件
│   ├── services/         # API服务
│   └── types/            # TypeScript类型定义
├── tests/                 # 测试文件
├── docs/                  # 项目文档
├── public/               # 静态资源（web-app模板）
├── agent.config.json     # 项目配置
├── package.json          # 依赖和脚本
├── tsconfig.json         # TypeScript配置
├── claude-progress.txt   # 人类可读进度日志
└── feature-list.json     # 结构化功能列表
```

## 🔧 核心命令速查

### 项目初始化
```bash
agent-cli init <项目名> [选项]
  --template, -t    项目模板 (web-app/api-service/library)
  --description, -d 项目描述
  --path, -p        项目路径
  --no-git          跳过Git初始化
  --yes, -y         非交互式模式
```

### 状态查看
```bash
agent-cli status [选项]
  --detailed, -d    显示详细信息
  --format, -f      输出格式 (text/json/html)
  --output, -o      输出文件路径
```

### 下一步功能
```bash
agent-cli next [选项]
  --feature-id, -f  指定功能ID
  --show-dependencies 显示依赖关系
```

### 测试运行
```bash
agent-cli test [选项]
  --report, -r      生成测试报告
  --output, -o      报告输出路径
  --format, -f      报告格式 (html/json)
```

### Web界面
```bash
# 启动Web服务器（默认端口3000）
agent-cli serve

# 指定端口
agent-cli serve --port 8080

# 浏览器访问
open http://localhost:3000
```

## 💡 实战示例

### 示例1：创建Todo应用
```bash
# 1. 初始化项目
agent-cli init todo-app --template web-app --yes

# 2. 进入项目
cd todo-app

# 3. 查看初始功能列表
agent-cli status

# 4. 开始第一个功能
agent-cli next
# 输出：推荐实现功能 "项目基础结构搭建"

# 5. 实现功能后运行测试
agent-cli test

# 6. 继续下一个功能
agent-cli next
```

### 示例2：API服务开发
```bash
# 1. 创建API项目
agent-cli init user-api --template api-service --yes

# 2. 增量开发循环
while true; do
  agent-cli next
  # 实现功能...
  agent-cli test
  git add . && git commit -m "feat: 实现新功能"
done
```

## 🚨 常见问题解决

### Q1: 初始化时提示路径不为空
```
解决方案：
- 使用空目录：agent-cli init my-project --path ./empty-folder/
- 清空目录：rm -rf existing-folder/* && agent-cli init my-project
```

### Q2: AI服务调用失败
```
检查步骤：
1. 验证API密钥：echo $ANTHROPIC_API_KEY
2. 检查网络连接
3. 切换AI提供商：agent-cli config --set "agent.ai.defaultProvider=openai"
```

### Q3: 测试运行失败
```
调试方法：
1. 查看详细错误：agent-cli test --verbose
2. 手动运行Puppeteer测试：node examples/web-app/scripts/run-tests.sh
```

### Q4: Web界面无法访问
```
排查步骤：
1. 检查端口占用：lsof -i :3000
2. 使用不同端口：agent-cli serve --port 8080
```

## 🛠️ 高级功能

### 配置管理
```bash
# 查看当前配置
agent-cli config --list

# 设置配置值
agent-cli config --set "agent.model=claude-3-5-sonnet"
agent-cli config --set "agent.temperature=0.8"

# 验证配置
agent-cli config --validate
```

### 报告生成
```bash
# 进度报告
agent-cli report --type progress --format html --output ./progress.html

# 测试报告
agent-cli report --type test --format json
```

### 上下文监控
```bash
# 检查token使用
agent-cli context --input ./messages.json --threshold 0.8

# 监控实时会话
agent-cli context --monitor --output ./context-log.txt
```

## 📊 进度跟踪文件说明

### `claude-progress.txt` - 人类可读进度日志
```
[2026-02-16 14:30:00] 开始项目: todo-app
[2026-02-16 14:35:00] 完成功能: 项目基础结构搭建
[2026-02-16 15:00:00] 测试通过: 基础结构测试 (5个测试)
```

### `feature-list.json` - 结构化功能列表
```json
{
  "features": [
    {
      "id": "feature-001",
      "name": "项目基础结构搭建",
      "description": "创建项目目录和配置文件",
      "status": "completed",
      "priority": "critical",
      "steps": ["创建目录结构", "配置TypeScript", "设置构建工具"]
    }
  ]
}
```

## 🎯 最佳实践

1. **保持功能原子性**: 每个功能应该独立可测试
2. **定期提交代码**: 每个功能完成后立即提交
3. **验证测试通过**: 每次修改后运行测试
4. **监控上下文使用**: 避免超出AI模型token限制
5. **使用模板系统**: 保持代码一致性

## 🔗 相关资源

- **完整用户指南**: `!doc/USER_GUIDE.md`
- **项目进度**: `!doc/PROGRESS.md`
- **任务清单**: `!doc/TODO.md`
- **示例项目**: `examples/web-app/`

## 📞 获取帮助

```bash
# 查看所有命令
agent-cli --help

# 查看命令详细帮助
agent-cli init --help
agent-cli test --help

# 查看版本信息
agent-cli --version
```

---

**立即开始:**
```bash
cd /Users/muliminty/project/agent-cli
npm run build
agent-cli init my-first-project
```

> 提示：本工具已通过70个测试用例验证，可放心用于生产环境。

*文档版本: 1.0.0*
*最后更新: 2026-02-18*